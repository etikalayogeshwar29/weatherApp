# weatherApp
WeatherNow is a simple weather web application built using HTML, CSS, and JavaScript. It uses the OpenWeather API to fetch real-time weather data and display the city name, temperature, humidity, wind speed, and weather conditions with matching icons. It also handles invalid city searches with an error message.
